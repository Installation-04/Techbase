{
  "name": "techbase",
  "version": "0.0.6",
  "private": true,
  "description": "Root package for Netlify Functions dependency resolution (netlify/functions/api.js). Netlify's function bundler only resolves deps from the site's top-level package.json, so backend/'s runtime dependencies are mirrored here too.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "scripts": {
    "test": "node --test backend/test"
  }
}
